--- fio-2.1.10.pristine/Makefile	2014-06-10 15:18:38.000000000 -0400
+++ fio-2.1.10/Makefile	2014-08-04 17:38:33.000000000 -0400
@@ -195,16 +195,19 @@
 endif
 
 ifeq ($(CONFIG_TARGET_OS), SunOS)
-	INSTALL = ginstall
+	INSTALL = /usr/gnu/bin/install
 else
 	INSTALL = install
 endif
-prefix = /usr/local
+prefix = /usr
 bindir = $(prefix)/bin
 
 ifeq ($(CONFIG_TARGET_OS), Darwin)
 mandir = /usr/share/man
 sharedir = /usr/share/fio
+else ifeq ($(CONFIG_TARGET_OS), SunOS)
+mandir = $(prefix)/share/man
+sharedir = $(prefix)/share/fio
 else
 mandir = $(prefix)/man
 sharedir = $(prefix)/share/fio
